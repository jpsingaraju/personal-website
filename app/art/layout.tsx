import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Art | Jathin Pranav Singaraju",
};

export default function ArtLayout({ children }: { children: React.ReactNode }) {
  return children;
}
